services:
  github-runner:
    image: ghcr.io/actions/actions-runner:latest
    restart: always
    command: ./run.sh
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - config:/home/runner/config
      - workspace:/home/runner/_work
    configs:
      - source: entrypoint.sh
        target: /entrypoint.sh
        mode: 0755
    entrypoint: /entrypoint.sh


configs:
  entrypoint.sh:
    content: |
      #!/bin/bash
      set -e
      cp -pr config/.runner .
      cp -p config/.path .
      cp -p config/.credentials .
      cp -p config/.credentials_rsaparams .
      exec "$@"


volumes:
  config:
  workspace:
